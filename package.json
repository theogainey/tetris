{
  "name": "tetris",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/esbuild src/index.ts --outfile=main.js --bundle --minify --sourcemap --target=chrome80,safari13 --loader:.ts=ts",
    "dev": "npx browser-sync start --server --files=\"*\""
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild": "0.17.9"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
